apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: industrial-edge
  title: "Industrial Edge: IoT Manufacturing (Multi-Cluster)"
  description: >
    Deploy an Industrial Edge IoT stack on a selected spoke (east/west).
    Runs under OSSM3 ambient mesh with ztunnel L4 metrics and Prometheus scraping.
  tags:
    - iot
    - kafka
    - edge
    - industrial-edge
    - multi-cluster
spec:
  owner: platform-engineering
  type: service
  parameters:
    - title: Edge Instance Configuration
      required:
        - name
        - owner
        - targetCluster
        - clusterDomain
      properties:
        name:
          title: Instance Name
          type: string
          default: edge-factory-1
          pattern: "^[a-z0-9-]+$"
          ui:autofocus: true
        owner:
          title: Owner
          type: string
          default: ${{ user.entity.metadata.name }}
        targetCluster:
          title: Target Cluster
          type: string
          enum: [east, west]
          enumNames: [East spoke, West spoke]
          default: east
        sensorCount:
          title: Machine Sensors
          type: integer
          default: 2
          enum: [1, 2]
        clusterDomain:
          title: Hub cluster apps domain
          type: string
          description: "e.g. apps.cluster-xxxxx.redhatworkshops.io"
          default: apps.cluster-xqg4c.dynamic2.redhatworkshops.io
  steps:
    - id: fetch-skeleton
      name: Generate Skeleton
      action: fetch:template
      input:
        url: ./skeleton
        values:
          name: ${{ parameters.name }}
          uniqueName: "${{ parameters.owner }}-${{ parameters.name }}"
          owner: ${{ parameters.owner }}
          namespace: "ie-${{ parameters.name }}-${{ parameters.targetCluster }}"
          targetCluster: ${{ parameters.targetCluster }}
          sensorCount: ${{ parameters.sensorCount }}
          clusterDomain: ${{ parameters.clusterDomain }}
    - id: publish
      name: Publish to Gitea
      action: publish:github
      input:
        repoUrl: "gitea-gitea.${{ parameters.clusterDomain }}?owner=ws-${{ parameters.owner }}&repo=${{ parameters.name }}-${{ parameters.targetCluster }}"
        description: "Industrial Edge ${{ parameters.name }} on ${{ parameters.targetCluster }}"
        defaultBranch: main
        repoVisibility: public
    - id: register
      name: Register in Catalog
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps['publish'].output.repoContentsUrl }}
        catalogInfoPath: /main/catalog-info.yaml
    - id: create-argocd-app
      name: Create ArgoCD Application
      action: http:backstage:request
      input:
        method: POST
        path: /api/proxy/k8s-api/apis/argoproj.io/v1alpha1/namespaces/openshift-gitops/applications
        headers:
          Content-Type: application/json
        body:
          apiVersion: argoproj.io/v1alpha1
          kind: Application
          metadata:
            name: "gen-${{ parameters.owner }}-${{ parameters.name }}-${{ parameters.targetCluster }}"
            namespace: openshift-gitops
          spec:
            project: default
            source:
              repoURL: "https://gitea-gitea.${{ parameters.clusterDomain }}/ws-${{ parameters.owner }}/${{ parameters.name }}-${{ parameters.targetCluster }}"
              targetRevision: main
              path: manifests
            destination:
              name: ${{ parameters.targetCluster }}
              namespace: "ie-${{ parameters.name }}-${{ parameters.targetCluster }}"
            syncPolicy:
              automated:
                selfHeal: true
                prune: true
              syncOptions:
                - CreateNamespace=true
    - id: notify
      name: Notify Owner
      action: http:backstage:request
      input:
        method: POST
        path: /api/notifications
        headers:
          Content-Type: application/json
        body:
          recipients:
            type: entity
            entityRef: "user:default/${{ parameters.owner }}"
          payload:
            title: "Industrial Edge '${{ parameters.name }}' deployed on ${{ parameters.targetCluster }}"
            description: "Your IoT edge instance is deploying via ArgoCD."
            link: ${{ steps['register'].output.entityRef }}
            severity: normal
  output:
    links:
      - title: Repository
        url: ${{ steps['publish'].output.remoteUrl }}
      - title: View in Catalog
        icon: catalog
        entityRef: ${{ steps['register'].output.entityRef }}
      - title: Open in DevSpaces
        icon: chat
        url: "https://devspaces.${{ parameters.clusterDomain }}/#https://gitea-gitea.${{ parameters.clusterDomain }}/ws-${{ parameters.owner }}/${{ parameters.name }}-${{ parameters.targetCluster }}"
