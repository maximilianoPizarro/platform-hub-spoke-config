# ${{ values.name }} (${{ values.environment }})

Industrial Edge Camel integration on spoke **${{ values.targetCluster }}**, editable in DevSpaces with Kaoto and Continue AI.

## Namespace

`${{ values.namespace }}`

## Routes

- MQTT → Kafka (Camel K)
- S3 data lake sink

Open **DevSpaces (Kaoto)** from the catalog entity links to edit routes visually.
