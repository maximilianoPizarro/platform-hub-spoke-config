*.tgz
charts/
# Vendored spoke chart (Argo must not fetch camel-tooling.github.io at sync time)
!components/camel-dashboard-openshift/charts/
!components/camel-dashboard-openshift/charts/*.tgz
!components/neuroface/charts/
!components/neuroface/charts/*.tgz
.DS_Store
agent-tools/

# Helm template scratch output (URLs belong in values — chart is developer-hub/templates/)
components/_dh-*.yaml

# Local ACS init-bundle YAML from roxctl (secrets — never commit)
hub-init.yaml

# Skupper AccessToken scratch files (contain grant codes — never commit)
.tmp/

# Manual Quay org-setup debug job (use GitOps template instead)
components/quay-registry/files/quay-workshop-org-setup-job.yaml

# Workshop screen recordings — local only, never commit
recordings/
workshop-recordings/
captures/
*.mp4
*.mkv
*.webm
*.mov

# Antora Showroom content — separate repo (maximilianoPizarro/showroom-hybrid-mesh-ai)
showroom-hybrid-mesh-ai/
.gemini_api_key
scripts/__pycache__/
