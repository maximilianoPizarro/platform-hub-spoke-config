*.tgz
charts/
.DS_Store
agent-tools/

# Helm template scratch output (URLs belong in values — chart is developer-hub/templates/)
components/_dh-*.yaml

# Local ACS init-bundle YAML from roxctl (secrets — never commit)
hub-init.yaml

# Skupper AccessToken scratch files (contain grant codes — never commit)
.tmp/
