clusterDomain: ""
channel: alpha

# primary = hub aggregator; agent = spoke Federated ETL agent (auto-set when clusterRole=spoke)
role: primary
clusterName: hub
clusterRole: hub

# Federated ETL object storage (MinIO deployed in industrial-edge-ml-workspace)
federatedStore:
  bucket: kubecost
  endpoint: minio.industrial-edge-ml-workspace.svc.cluster.local:9000
  region: us-east-1
  accessKey: minio
  secretKey: minio123
