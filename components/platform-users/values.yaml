# Workshop user scale (profiles: 30, 50, 100, 200)
userCount: 50
defaultPassword: "Welcome123!"
# Hub only — view ACM, Kiali, Skupper in console without cluster-admin
grantClusterReader: false
htpasswdSecretName: htpasswd-users
identityProviderName: htpasswd_users
