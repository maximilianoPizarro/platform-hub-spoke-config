apiVersion: v2
name: ie-anomaly-alerter
description: Industrial Edge anomaly alerter — monitors sensor vibration/temperature via MQTT and sends alerts to Mailpit
type: application
version: 0.1.0
appVersion: "1.0.0"
