apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: industrial-edge-lb
  namespace: hub-gateway-system
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  parentRefs:
    - name: hub-gateway
      namespace: hub-gateway-system
  hostnames:
    - industrial-edge.{{ .Values.clusterDomain }}
  rules:
    - backendRefs:
        {{- if .Values.clusters.east.domain }}
        - name: industrial-edge-east
          port: 443
          weight: {{ .Values.gateway.weights.east }}
        {{- end }}
        {{- if .Values.clusters.west.domain }}
        - name: industrial-edge-west
          port: 443
          weight: {{ .Values.gateway.weights.west }}
        {{- end }}
