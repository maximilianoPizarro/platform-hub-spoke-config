# Optional manual overlay — RHDP templates values.yaml directly (no extra -f needed).
# Use only for local helm: helm upgrade ... -f values.yaml -f values-rhdp.yaml --set deployer.domain=...
# Content is duplicated in values.yaml deployer/clusters/litemaas sections.
